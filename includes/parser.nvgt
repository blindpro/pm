void parse()
{
string[] args = cli.args;
if(args.empty())
{
output("usige.");
output("New <name>. Create a new project using the given name.");
output("sync. Sync current project with git.");
return;
}
string base = args[0];
for(int i = 0; i<commands.length(); i++)
{
if(commands[i].name == base)
{
commands[i].exsacute();
break;
}
}
output(base+" is not a pm command.");
}