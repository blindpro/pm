void parse()
{
string[] args = cli.args;
if(args.empty())
{
output("usage. New <name>: Create a new project with the given name. sync. Sync project with git.");
return;
}
string base = args[0];
if(base == "sync")
{
sync(args);
}
else if(base == "new" and !is_blank(args[1]))
{
new(args);
}

}